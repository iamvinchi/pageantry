<%- include('../partials/header') %>
<div class="card text-center">
    <div class="card-body">
        <h1 class="card-title text-success">Thank You for Voting!</h1>
        <div class="my-4">
            <i class="bi bi-check-circle-fill text-success" style="font-size: 5rem;"></i>
        </div>
        <h3>You've successfully voted for <%= vote.contestant.fullName %></h3>
        <p class="card-text">Transaction Reference: <%= vote.transactionReference %></p>
        <p class="card-text">Amount: ₦<%= vote.amount %></p>
        <p class="card-text">Date: <%= vote.voteDate.toLocaleString() %></p>
        
        <div class="mt-4">
            <a href="/votes/<%= vote.contestant.shareableLink %>" class="btn btn-primary me-2">
                Vote Again
            </a>
            <a href="/contestants" class="btn btn-secondary">
                View Other Contestants
            </a>
        </div>
    </div>
</div>
<%- include('../partials/footer') %>